{
  "name": "estimation-gym-app",
  "version": "1.8.1",
  "private": true,
  "description": "Daily Fermi-estimation puzzle as an installable web app, sharing its scoring logic and question bank with the Omarchy bar widget.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "test": "node core/Model.test.js && node core/questions.test.js && node presenter.test.js && node storage.test.js && node version.test.js && node sw.test.js && node install.test.js && node markup.test.js && node smoke/run.mjs",
    "serve": "node scripts/serve.mjs",
    "sync-core": "node scripts/sync-core.mjs"
  }
}
