The scoring key and the numbered steps inside How to play.

```jsx
<ScoringTable compact />
<StepList steps={HOW_TO_PLAY.steps} />
```

Take the step copy from `core/Model.js` HOW_TO_PLAY rather than rewriting it — the Omarchy widget teaches the same rules in the same words. Never hard-code points: they come from BANDS.
